db.run(`CREATE TABLE xp (
            _id INTEGER PRIMARY KEY AUTOINCREMENT,
            guild_id INTEGER,
            user INTEGER,
            xp INTEGER,
            level INTEGER
            )`, (err) => {
                if(err) console.log(err)
            })

db.run(`CREATE TABLE prefix (
            _id INTEGER PRIMARY KEY AUTOINCREMENT,
            guild_id INTEGER,
            prefix text
            )`,

db.run(`create table message_cooldown (
            _id INTEGER PRIMARY KEY AUTOINCREMENT
            user INTEGER,
            time INTEGER
        )`)